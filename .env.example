# Backend listens locally; LAN testing can set HOST=0.0.0.0
HOST=127.0.0.1
PORT=8796
# Optional. Without this, rooms/history are temporary and reset on restart.
MYSQL_URL=
# Douyin production requires an actual WSS server. No fake public domain is supplied.
MINIGAME_SERVER_URL=
DOUYIN_APP_ID=
